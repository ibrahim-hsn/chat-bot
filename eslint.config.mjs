// Root ESLint entrypoint. The full configuration lives in `@one2one/config`
// so that every workspace shares a single source of truth.
import config from "@one2one/config/eslint";

export default config;
